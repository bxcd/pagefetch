package art.coded.pagefetch.model.fetch;

/**
 * The constants required to interact with the datasource in order to fetch raw Elements
 */
public class ElementDatasourceContract {

    // Default values (replace null)
    public static final String DEFAULT_VALUE_STR = "";
    public static final int DEFAULT_VALUE_INT = -1;

    // Response keys
    public static final String KEY_ID = "id";
    public static final String KEY_LISTID = "listId";
    public static final String KEY_NAME = "name";
}